
<!-- Initialize i18n at index page -->

<!doctype html>
<html>
    <head>
        <script>
            import { languages } from "../i18n/ui";

            const preferredLang: string = navigator.language.slice(0,2);

            for(let i = 0; i < languages.length; i++) {
                if(languages[i] === preferredLang) {
                    window.location.href = "/" + preferredLang;
                } else {
                    window.location.href = "/en";
                }
            }
        </script>
    </head>
    <style>
        html {
            background: rgb(40,40,40);
        }
    </style>
</html>
